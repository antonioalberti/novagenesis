# M0 Freeze Receipt — v1.0.0 profile v4

**Date:** 2026-09-14
**Branch:** `AIOPT3`
**Functional candidate SHA:** `c7deb7de6c43e8e64121aedd320c9f684233a940`
**Receipt publication boundary:** subsequent documentation-only commits on `AIOPT3` (the current branch remains a descendant of the functional candidate)
**Remote ref at last verification:** `origin/AIOPT3` at `b81ece8353b82d23e0c87795746b681088c8314d`
**Worktree:** clean at receipt creation
**Plan:** `RELEASE-MASTER-PLAN-v1.0.0` version 4

## Supported profile

- Linux/AIOPT3
- two Alpine guests dedicated exclusively to NovaGenesis testing
- current remote entrypoint `Scripts/AlpineVMs/ng_remote_executor.py`
- normal NRNCS profile
- one normal subscription path
- Source → PGCS → NRNCS → Repository
- five deterministic JPEGs with independent name/size/SHA-256 maps
- bounded lifecycle, provenance/build receipt, durable evidence and identity-based teardown

## Explicitly deferred to v1.1+

- generic local-mode release claim
- `ng_elc.py` rename (SPEC-057)
- complete subscription error matrix
- discovery-only independent guarantee
- 100/1000-photo, stress, soak and performance claims
- full legacy-profile parity and multiplatform support
- AIOPT4

## Credential fixture decision

The historical Alpine password remains classified as compromised and is absent from active public files. External rotation is **N/A for this v1.0 profile only** because the referenced guests are dedicated NovaGenesis test fixtures with no production or unrelated use. A fixture credential, if required by the local ignored operator configuration, must never be committed, published as a secure default, reused outside the fixtures, or retained if a guest is repurposed.

## Verified at freeze

- `git status --short --branch`: clean, branch `AIOPT3`
- local HEAD equalled `origin/AIOPT3` at the receipt publication boundary
- `git diff --check`: PASS
- Plano Mestre v4 recognizes four operational gates M0–M3
- SPEC-038 records the bounded fixture-only credential decision
- Existing R12–R14 implementation and test commits are ancestors of the candidate

## Open M0 conditions

This receipt freezes the functional candidate boundary; it does **not** mark M0 PASS. Subsequent documentation-only descendants must not change the functional scope. Remaining conditions are current-candidate hygiene/public-data classification, historical-document classification, normal-runtime reproducibility after cleanup, SPEC-054 review and a clean read-back audit. The dedicated-test-fixture credential decision is recorded as a bounded scope decision, not as a production-security approval.
