# G0 freeze receipt — AIOPT3 v1.0.0 candidate

**Date:** 2026-09-14
**Branch:** AIOPT3
**Source freeze commit:** `18d9b18`
**Remote before publication:** `origin/AIOPT3 = 1af604d`
**Scope decision:** release work only; performance/historical work preserved outside the candidate

## Result

The release candidate was separated from the dirty working tree and committed as `18d9b18` (`chore(release): freeze AIOPT3 v1.0.0 candidate`). The worktree was clean after the commit.

The excluded performance material (NG-039–045 implementation/spec/result files and the local Hermes plan artefact) was moved to a reversible operator backup outside the repository. No excluded file was deleted.

## Validation

- Python compilation: **21 files, PASS**
- Shell syntax: **25 files, PASS**
- NG-ELC/Alpine test suite: **62/62 PASS**
- CMake Debug build, out-of-tree: **PASS**
- CMake Sanitizer build, out-of-tree: **PASS**
- `git diff --check` on the frozen commit: **PASS**
- secret-like pattern scan of staged candidate: **0 matches**

Build artefact hashes are recorded in the session execution output; the build directories were `/tmp/ng-build-18d9b18-debug` and `/tmp/ng-build-18d9b18-sanitizer`. They are diagnostic build outputs, not release evidence bundles.

## Open items

- The candidate was not yet pushed when this receipt was written; remote publication must be verified before the guest build.
- G2 still requires the canonical NG-ELC build/smoke on this immutable candidate.
- Compiler warnings remain classified, not accepted: the build includes existing unused-variable/sign-compare warnings and a `-Wmismatched-new-delete` warning in `Common/src/Message.cpp`.
- G1 and G3–G10 remain open or blocked according to the master plan.
