# SPEC-044 post-fix qualification result

Date: 2026-09-12
Branch: AIOPT3
Source revision: working tree based on `e8faaf3`

## Production change

Allowlisted file/function:

```text
PGCS/src/CoreRunEvaluate01.cpp
CoreRunEvaluate01::CheckSubscriptions()
```

The invalid cast from the Core action owner to `PG*` was replaced by lookup of the sibling `PG` block through `PB->PP->GetBlock("PG", ...)`, followed by a checked `dynamic_cast`. Missing or wrong-type lookup is fail-closed. Peer-dependent processing now requires a resolved peer; unresolved deletion is handled separately.

## Build qualification

```text
CMake RelWithDebInfo: PGCS + ContentApp — PASS
CMake Sanitizer O1:   PGCS + ContentApp — PASS
CMake Debug:          PGCS + ContentApp — PASS
```

## Matched runtime smoke

Command:

```text
timeout 5s ./cmake-build-sanitizer/PGCS ./IO/PGCS/ 0 Intra_Domain -lc
```

Result:

- exit status 124: expected controlled timeout;
- Core/Gateway startup reached;
- target diagnostic `CoreRunEvaluate01.cpp:425`: ABSENT;
- ASan errors: 0;
- no UBSan marker observed in this smoke;
- log: `/tmp/spec044-pgcs-sanitizer-postfix.log`.

An instrumented Sanitizer build (`-DDEBUG`, separate build directory) provided explicit traversal evidence:

```text
check_subscriptions_marker=1
target_ubsan=ABSENT
asan_errors=0
run_exit=124 (controlled timeout)
```

Log: `/tmp/spec044-pgcs-sanitizer-debuglog.log`.

## Provenance hashes

```text
PGCS/src/CoreRunEvaluate01.cpp aa63fed0ded17fb9d813ed3c7b9bdc8cd645be9a88133143f9402d64d43796de
cmake-build-performance/PGCS   ec6a2f941887be7b0cecafa836b39865f65cbc1b385d078230692fd9cee98397
cmake-build-performance/ContentApp 865af7f5e22f390fc705c1e958d1c8bf8193f89e651751028e6b7e1d9ebc215f
cmake-build-sanitizer/PGCS     767f256d9639e4aba1b1faa490159ee5a0f1a85a508a0a952d04f3f70081c066
cmake-build-sanitizer/ContentApp e15b9cf74458ff94221973e57ba750525d68270e7186ba8b1643d56067e84d55
```

## Remaining gates

Functional subscription fixtures with known/unknown publishers, PG PID overlap, wrong/missing sibling block, unresolved non-NULL BID, and deletion semantics remain to be executed. The instrumented smoke proves traversal but does not populate the subscription branches. This result is not final acceptance or a performance claim.

`ctest --test-dir cmake-build-sanitizer --output-on-failure` returned `No tests were found!!!`; no existing test registration can be used as functional evidence. A real subscription fixture or E2E execution path is therefore still required.

The sibling `PG` and `Core` blocks are both created by `PGCS::NewBlock()` and retained in the same process block registry. `Process::GetBlock()` returns the registered block pointer; no block removal occurs during the evaluated startup path. The checked cast is therefore bounded to the existing registry object, but concurrent registry mutation has not been stress-tested.

## Local E2E deployment smoke

The Sanitizer binaries were started in documented order: `PGCS -lc`, `NRNCS`, Repository `ContentApp`, Source `ContentApp`, each with an independent bounded timeout. All four returned exit 124 from their timeout wrappers and reached their service/Core startup markers. No `ERROR`, `ALARM`, ASan, UBSan or `SEGV` marker was present in the four logs. The run created no subscription-specific markers or publisher state, so it is deployment smoke only, not SPEC-044 functional GREEN.

Logs: `/tmp/spec045-e2e-20260912/{pgcs,nrncs,repository,source}.log`.

The post-run cleanup removed all 16 residual shared-memory segments and 16 named semaphores; no NovaGenesis processes or IPC resources remained.

## Local E2E with one synthetic photo

The same bounded four-process scenario was repeated after generating one controlled `100x100` JPEG in `IO/Source1`. All processes reached their startup/Core markers and returned exit 124 from their timeout wrappers. The source JPEG remained in `IO/Source1`, but the logs contained no publication, subscription, payload, delivery or `Check subscriptions` markers. Sanitizer/error marker counts were zero. This is negative evidence that the current scenario did not reach the functional stimulus, not functional GREEN.

Logs: `/tmp/spec045-e2e-photo-20260912/{pgcs,nrncs,repository,source}.log`.

Cleanup was verified: 0 NovaGenesis processes, 0 shared-memory segments and 0 semaphore arrays remained.

## Long-window E2E with one synthetic photo

The same four-process Sanitizer scenario was repeated with a 55-second PGCS window and 51–53-second application windows, exceeding `DelayBeforePublishingServiceOffer 30`. All wrappers returned 124. Startup/Core markers were present, but the logs contained no publication, subscription, payload, delivery or evaluation markers. The source JPEG remained present; all RTT/telemetry files remained zero bytes. Sixteen shared-memory segments were removed by cleanup, leaving 0 NovaGenesis processes, 0 shared-memory segments and 0 semaphore arrays.

This is a readiness/observability failure of the current E2E stimulus, not evidence that the SPEC-044 functional branches pass or fail.

Logs: `/tmp/spec045-e2e-photo-long-20260912/{pgcs,nrncs,repository,source}.log`.
