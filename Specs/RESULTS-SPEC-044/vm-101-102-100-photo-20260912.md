# VM 101/102 runtime evidence — 2026-09-12

## Provenance

- Branch: `AIOPT3`
- Commit on both guests: `21a5512b0dc34df0c7f730805e55637144087073`
- VM 101: Repository, `192.168.0.61`
- VM 102: Source, `192.168.0.36`
- Binaries rebuilt independently on both guests with normal CMake profile.
- `git fsck --full --no-progress`: no errors on either guest.
- Executables: Alpine x86-64 static-pie, debug information present.

## Runtime sequence

1. VM 101 and VM 102 were stopped/started before the run.
2. PGCS started on both guests with canonical lowercase peer MACs.
3. Discovery verified on Proxmox bridge with EtherType `0x1234` in both directions.
4. NRNCS started on VM 102.
5. Repository ContentApp started on VM 101.
6. Source launcher started last on VM 102 with a fresh immutable staging run `dd14bb9ef5a340acad3f9d4b50119df9` containing 100 photos.

## Delivery reconciliation

Programmatic SHA-256 map comparison:

```text
Source staging: 100 JPEGs
NRNCS cache:    100 JPEGs
Repository:     100 JPEGs
Source == NRNCS:     MATCH
Source == Repository: MATCH
NRNCS == Repository: MATCH
Source missing in Repository: 0
Repository extra files:       0
```

The transport capture showed control traffic and payload-sized frames, with no packet drops reported by tcpdump. No current-path error log was present. Historical logs containing errors were excluded from this result because their modification times and paths belonged to earlier runs.

## Teardown

After collection, both guests were cleaned with the NG cleanup script and stopped. Final verification:

```text
VM 101: stopped
VM 102: stopped
NovaGenesis processes: 0
Shared-memory segments: 0
Semaphore arrays: 0
```

## Scope classification

This is a verified cross-VM normal-path photo-delivery result for the rebuilt commit. It does not validate the SPEC-044 subscription matrix (known/unknown publishers, PID overlap, lookup failure, BID policy or deletion semantics), which remains open.
