# G1 Historical Classification — v1.0 profile v4

**Date:** 2026-09-14
**Tree:** `670af9b5764ea23376418b45404da930cda50aaf`
**Scope:** active v1.0 profile and retained records that could be mistaken for active instructions

## Classification inventory

- `14` `Specs/RESULTS-*` directories containing `5373` retained result/evidence files: classified as retained evidence by path and not active deployment instructions.
- `16` files under `Docs/DECISIONS`: reviewed as decision records; the baseline restoration record carries an explicit `HISTORICAL RECORD` banner.
- `3` files under `Docs/DIAGNOSTICS`: classified as diagnostic/investigative records, not supported v1.0 procedures.
- `17` files under `Scripts/Simple`: classified as legacy/diagnostic tooling outside the v1.0 supported entrypoint; the v1.1+ backlog preserves the broader legacy review.
- Active v1.0 instructions remain under `Scripts/AlpineVMs`, `Scripts/ProjectAudit`, the v4 release plan and the linked v1.0 evidence contract.

## Disposition

Historical/result/diagnostic material is retained for provenance and reproducibility but does not override active v1.0 instructions. Legacy scripts remain available for later work and are not advertised as supported v1.0 interfaces. No history rewrite or deletion was performed.

## Result

Historical classification for the **v1.0 supported profile**: **PASS**. This does not claim that every historical or legacy artifact is suitable for production, nor does it close the v1.1+ legacy cleanup backlog.
